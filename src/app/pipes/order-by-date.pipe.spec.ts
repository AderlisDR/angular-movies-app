/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { OrderByDatePipe } from './order-by-date.pipe';

describe('Pipe: Datee', () => {
  it('create an instance', () => {
    let pipe = new OrderByDatePipe();
    expect(pipe).toBeTruthy();
  });
});
